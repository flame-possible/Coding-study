#include <bits/stdc++.h>

using namespace std;

int main(){

        ios_base::sync_with_stdio(false);
        cin.tie(0);

        double a, b;

        cin >> a >> b;

        double result = (a + b) * (a - b);

        cout << result << '\n';


        return 0;

}
