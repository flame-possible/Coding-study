#include <bits/stdc++.h>

using namespace std;

int main(){

        ios_base::sync_with_stdio(false);
        cin.tie(0);

        char input[101];

        cin >> input;

        int result = strlen(input);

        cout << result << '\n';

        return 0;

}
